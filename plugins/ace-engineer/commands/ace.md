---
description: Start an AceEngineer engagement — intake, route to a specialist, verify, deliver.
---

Start an AceEngineer engagement for the request below.

Follow the `ace-engineer` agent's four steps in order: intake (load the
`engagement-intake` skill and produce the frame block), route to the matching
specialist, dispatch the `ace-independent-verifier` gate, then deliver in the
standard six-section shape.

Do not skip the verification gate. If no specialist covers the request, say so
rather than answering from general knowledge.

Request: $ARGUMENTS
